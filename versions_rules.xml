<ruleset comparisonMethod="maven"
         xmlns="http://mojo.codehaus.org/versions-maven-plugin/rule/2.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://mojo.codehaus.org/versions-maven-plugin/rule/2.0.0 http://mojo.codehaus.org/versions-maven-plugin/xsd/rule-2.0.0.xsd">
    <rules>
        <rule groupId="org.apache.maven" comparisonMethod="maven">
            <ignoreVersions>
                <!-- Ignore Pre-Releases -->
                <ignoreVersion type="regex">.*-alpha.*</ignoreVersion>
                <ignoreVersion type="regex">.*-rc.*</ignoreVersion>
                <ignoreVersion type="regex">.*-beta.*</ignoreVersion>
            </ignoreVersions>
        </rule>

        <rule groupId="com.fasterxml.jackson.core" comparisonMethod="maven">
            <ignoreVersions>
                <!-- Ignore Pre-Releases -->
                <ignoreVersion type="regex">.*-rc.*</ignoreVersion>
                <ignoreVersion type="regex">.*-alpha.*</ignoreVersion>
                <ignoreVersion type="regex">.*-beta.*</ignoreVersion>
            </ignoreVersions>
        </rule>

        <rule groupId="org.openjfx" comparisonMethod="maven">
            <ignoreVersions>
                <!-- Ignore Early Access and Pre-Release builds -->
                <ignoreVersion type="regex">.*-ea.*</ignoreVersion>
                <ignoreVersion type="regex">.*-alpha.*</ignoreVersion>
                <ignoreVersion type="regex">.*-beta.*</ignoreVersion>
                <ignoreVersion type="regex">.*-rc.*</ignoreVersion>
            </ignoreVersions>
        </rule>

        <rule groupId="org.kohsuke" comparisonMethod="maven">
            <ignoreVersions>
                <!-- Ignore Pre-Releases -->
                <ignoreVersion type="regex">.*-rc.*</ignoreVersion>
                <ignoreVersion type="regex">.*-alpha.*</ignoreVersion>
                <ignoreVersion type="regex">.*-beta.*</ignoreVersion>
            </ignoreVersions>
        </rule>

        <rule groupId="org.apache.maven" artifactId="maven-model" comparisonMethod="maven">
            <ignoreVersions>
                <!-- Ignore Pre-Releases -->
                <ignoreVersion type="regex">.*-alpha.*</ignoreVersion>
                <ignoreVersion type="regex">.*-rc.*</ignoreVersion>
                <ignoreVersion type="regex">.*-beta.*</ignoreVersion>
            </ignoreVersions>
        </rule>
    </rules>
</ruleset>
